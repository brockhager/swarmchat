# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp.*
/.parcel-cache
/.next
/dist
/build
/out
/public/build

# Tauri / Rust
/target
/src-tauri/target
# do not ignore Cargo.lock for apps running reproducible builds — keep it in repo
# ignore sidecar binaries (packaged or built locally)
/src-tauri/**/*.exe
/src-tauri/**/*.dll
/src-tauri/**/*.so
/src-tauri/**/*.dylib
/src-tauri/binaries/
/resources/sidecar/

# Local env files
.env
.env.*

# Editor/IDE
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp
*~

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
logs/
coverage/

# Test output
/coverage
/jest

# Misc
dist-ssr/
.cache/
*.tgz

# lock files for package managers — keep them in repo unless intentionally ignored

# keep sensitive or local config out
.local

# Optional: depending on your workflow
.env.local
.env.development.local
.env.test.local
.env.production.local
